[project]
name = "jupyter-kernel"
version = "0.1.0"
description = "Jupyter-JSC kernel with uv"
requires-python = ">=3.12,<3.13"

dependencies = [
    "ipykernel",
    "pyzmq",
    "jupyter-client",
    "numpy",
    "pandas",
    "scipy",
    "matplotlib",
]